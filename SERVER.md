# Instruções para rodar o servidor

Comandos:

```sh
# Instalar todos os pacotes
npm i
# Configure o .env.local
npm run migrate
# Seed é opcional
npm run seed

# build do Next.js
npm run build
npm start
```
